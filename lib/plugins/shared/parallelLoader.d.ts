import { Builder } from '../../Builder';
import Spin from '../../Spin';
export declare const hasParallelLoalder: (builder: Builder) => boolean;
export declare const addParalleLoaders: (builder: Builder, spin: Spin, compilerRules: any) => any;
